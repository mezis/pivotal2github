require 'spec_helper'

describe Pivotal2github do
  it 'has a version number' do
    expect(Pivotal2github::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
